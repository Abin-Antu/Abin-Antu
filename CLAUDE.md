# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Repo Is

GitHub profile README repo (`Abin-Antu/Abin-Antu`). Single file: `README.md`. Rendered automatically on the GitHub profile page.

## Editing Guidelines

- All content lives in `README.md` — no build step, no deploy needed.
- Badge URLs use `shields.io` (`img.shields.io/badge/...`) format with `style=for-the-badge`.
- GitHub stats widgets come from:
  - Streak stats: `nirzak-streak-stats.vercel.app`
  - Language stats / general stats: `github-readme-stats.vercel.app`
  - Visit counter: `visitcount.itsvg.in`
- Keep `username=Abin-Antu` consistent across all widget URLs.
- Preview rendering: push to GitHub or paste raw Markdown into a local renderer — no local tooling exists.
